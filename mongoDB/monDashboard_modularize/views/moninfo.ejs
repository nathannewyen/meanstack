<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <% for (var info in data) { %>
    <title><%= data[info].name %></title>
    <% } %>
</head>

<body>
    <nav>
        <a href="/" class="text-dark"> Back to Dashboard </a>
    </nav>
    <div class="container" id="mongoosesInfo">
        <% for (var info in data) { %>
        <h3>Name: <%= data[info].name  %> </h3>
        <h4>Age: <%= data[info].age  %></h4>
        <p> <i> Created At: <b> <%= moment(data[info].createdAt).format("hh:mm a, MMMM DD YYYY")  %> </i> </b> </p>

        <% if (data[info].updatedAt) { %>
        <p> <i> Updated At: <b> <%= moment(data[info].updatedAt).format("hh:mm a, MMMM DD YYYY") %> </i> </p>
        <% } else { %>
        <p> No update</p>
        <% } %>

        <button class="btn btn-success"><a href="/mongooses/edit/<%= data[info].id  %>" class="text-light"> Edit
            </a></button>
        <% } %>



        <% for (var info in data) { %>
        <form action="/mongooses/destroy/<%= data[info].id  %>" method="post" id="delete">
            <button class="btn btn-danger">
                Delete
            </button>
        </form>
        <% } %>
    </div>
</body>

</html>