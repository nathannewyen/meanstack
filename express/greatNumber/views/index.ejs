<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <title>Document</title>
</head>

<body>
    <div class="container text-center bg-light">
        <h2> Welcome to the great number</h2>
        <p>I am thinking of a number between 1 and 100</p>
        <p>Take a guess</p>
        <%if (input.num > context.randomNum) { %>
        <p class="bg-danger"> Too High! Random Number was <%= context.randomNum  %> </p>
        <% } else if (input.num > 0 && input.num < context.randomNum) {  %>

        <p class="bg-danger"> Too Low! Random Number was <%= context.randomNum  %> </p>
        <% } else if (input.num < 0 )  { %>

        <p class="bg-danger">Please enter a number greater than 0</p>
        <% } else  if (input.num == context.randomNum) { %>

        <p class="bg-success"> Congrats <%= input.num %> was a number </p>
        <%  } %>

        <form action="/random" method="post">
            <input class="form-control text-center" name="number" type="number"
                placeholder="Your input number was <%= input.num %>" required>

            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</body>

</html>